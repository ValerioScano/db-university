Metto il COUNT con le sue parentesi per verificare che il numero coincida con il risultato atteso

***1***

SELECT COUNT(*)
FROM students 
WHERE YEAR(date_of_birth) = 1990;

***2***

SELECT COUNT(*)
FROM courses
WHERE cfu > 10

***3***

soluzione trovata da me 

SELECT *
FROM students
WHERE to_days(curdate()) - to_days((date_of_birth)) >= 10958

soluzione trovata smanettando internet e IA

SELECT COUNT(*)
FROM students
WHERE date_of_birth <= DATE_SUB(CURDATE(), INTERVAL 30 YEAR);

stesso risultato però!